# ══════════════════════════════════════════════════════
#  SI Unit Converter — Requirements
#  Install: pip install -r requirements.txt
# ══════════════════════════════════════════════════════

# ──Core─────────
pint>=0.22
numpy>=1.24
pandas>=2.0
scipy>=1.11

# ──Streamlit Web App (app_si.py)─────────────────────
streamlit>=1.32
plotly>=5.18
openpyxl>=3.1          # read/write Excel files

# ──REST API(api.py)─────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0          #fastapi

# ──Automated Testing(tests/)─────────────────────────
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27            #test API client (use with fastapi TestClient)